
#file conversation_dsl_text.rb

def conversation(&block)
  app = Conversation.new
  app.instance_eval(&block)
end

class ConversationText
  def say(message)
    puts message
  end
end


require 'conversation_dsl_text'

#basic validation
validates_numericality_of :number_of_apples

conversation do
  say 'hello' 
  
  ask :number_apple, "how many apples do you want?"

  validate do

  end

  condition_if ('apples > 10 and cars = 4') do
    hide_field(:backpacks)
  end

  collect color_of_apples
  collect likes_apples
  
  if likes_apples and number_of_apples > 3 
    collect social_security_number
  end
  
  say goodbye
end


text message => network => smsc => web hook => rails app => responds with text => api => smsc => network => cellphone

user -> sms
sms -> api

puts "hello"
puts "number of apples?"
gets (number_of_apples)


<form>
<label>how many apples do you want?</label>
<input name="num_apples">
